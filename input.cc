#include "input.h"


Input::input(Interface * interface){
    _interface = interface;
}

void movePlayer(Direction direc){
    switch (direc)
    {
    case UP:
        /* code */
        break;
    case LEFT:
        /* code */
        break;
    case DOWN:
        /* code */
        break;
    case RIGHT:
        /* code */
        break;
    default:
        break;
    }
}

void shootPlayer();

void pause();

void reset();

